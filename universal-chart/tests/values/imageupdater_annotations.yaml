imageUpdaters:
  my-app:
    applicationName: "test"
    annotations:
      note: my-annotation
    images:
      - alias: app
        imageName: nginx
        manifestTargets:
          helm:
            name: deployments.app.image
            tag: deployments.app.imageTag
