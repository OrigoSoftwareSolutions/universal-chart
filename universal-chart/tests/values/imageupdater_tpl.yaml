imageUpdaters:
  my-app:
    applicationName: "{{ .Release.Name }}-app"
    images:
      - alias: app
        imageName: nginx
        manifestTargets:
          helm:
            name: deployments.app.image
            tag: deployments.app.imageTag
