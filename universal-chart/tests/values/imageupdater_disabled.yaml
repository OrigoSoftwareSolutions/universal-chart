imageUpdaters:
  my-app:
    disabled: true
    applicationName: "ignored"
    images:
      - alias: app
        imageName: nginx
        manifestTargets:
          helm:
            name: deployments.app.image
            tag: deployments.app.imageTag
