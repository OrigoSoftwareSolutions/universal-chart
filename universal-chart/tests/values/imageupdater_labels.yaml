imageUpdaters:
  my-app:
    applicationName: "test"
    labels:
      custom: my-label
    images:
      - alias: app
        imageName: nginx
        manifestTargets:
          helm:
            name: deployments.app.image
            tag: deployments.app.imageTag
